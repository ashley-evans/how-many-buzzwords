schema {
    query: Query
}

type Query {
    keyphrases(baseURL: ID!): [Keyphrase]!
}

interface Node {
    id: ID!
}

type Keyphrase implements Node {
    id: ID!
    pathname: String!
    keyphrase: String!
    occurrences: Int!
}
